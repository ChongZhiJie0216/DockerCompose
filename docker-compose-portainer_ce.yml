version: '3.3'
services:
    portainer:
        ports:
            - '9000:9000'
        restart: always
        container_name: portainer
        volumes:
            - '/var/run/docker.sock:/var/run/docker.sock'
            - '/home/jiecloud/Docker/portainer/:/data'
        image: portainer/portainer-ce:latest
